version: '3.8'

services:
  nira_guardian:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nira_guardian
    ports:
      - "8000:8000"
      - "53:53/udp"
      - "5353:5353/udp"
    environment:
      - CORS_ORIGINS=["http://localhost:8000"]
    restart: always
